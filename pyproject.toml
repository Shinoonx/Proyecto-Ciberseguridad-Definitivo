[project]
name = "vulnerability-miner"
version = "0.1.0"
description = "Miner de vulnerabilidades automatizado con CodeQL"
requires-python = ">=3.10"
dependencies = [
    "typer>=0.9.0",
    "pydantic>=2.0.0",
    "requests>=2.31.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0"
]  